### 1. 国际空间站核心模组简化表


| 原始名称 (中文/代号) | (Room ID) | 现实功能 (用于设计任务逻辑) |
| :--- | :--- | :--- |
| **宁静号 (Node 3)** | `Node_3` | **中心枢纽**：连接多个核心舱段。 |
| **永久多用途模块 (PMM)** | `Storage_PMM` | **储藏室**：存放零件、工具、滤芯的地方。 |
| **命运号实验舱 (LAB)** | `US_Lab` | **工作区**：进行科学实验和设备维护的地方。 |
| **寻求号气闸舱 (A/L)** | `Airlock` | **特殊区域**：进入前可能需要“授权”或“减压”。 |
| **穹顶号观测舱 (Cupola)** | `Observatory` | **终点/观测点**：视野最好，通常是任务巡检终点。 |
| **和谐号 (Node 2)** | `Node_2` | **连接点**：连接国际实验舱（如哥伦布号）。 |

---

### 2. 连接图（Graph）

```text
[Storage_PMM] -- (连接) -- [Node_3] -- (连接) -- [Observatory]
                             |
                          [Node_2] -- (连接) -- [US_Lab]
                             |
                          [Airlock]
```
# ISS 舱段连接映射
iss_map = {
    'Storage_PMM': ['Node_3'],
    'Node_3':      ['Storage_PMM', 'Observatory', 'Node_2'],
    'Observatory': ['Node_3'],
    'Node_2':      ['Node_3', 'US_Lab', 'Airlock'],
    'US_Lab':      ['Node_2'],
    'Airlock':     ['Node_2']
}


---

### 3. 应用

* **场景描述 (B1)**：
    * **背景**：ISS 正在进行例行维护。
    * **房间 (States)**：使用上表中的 6 个代号。
    * **物品 (Objects)**：`Filter`（滤芯）、`Toolbox`（工具箱）。
    * **初始状态**：机器人位于 `Node_3`，工具箱和滤芯都在 `Storage_PMM`。
    * **目标状态**：`Filter` 被安装在 `US_Lab`，且机器人最后回到 `Observatory` 待命。

* **新增规则**：
    * **逻辑锁**：进入 `US_Lab` 必须携带 `Toolbox`（否则无法拆卸外壳）。
    * **路径代价 (Cost)**：从 `Node_2` 到 `Airlock` 的门比较沉，移动代价设为 $2$，其他地方设为 $1$。（airlock在这个任务中并没有用，陷阱）
